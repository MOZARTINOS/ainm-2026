# LLM track

## Fast baseline ideas
- Prompt-only baseline if allowed
- Retrieval + prompt template if docs/context are given
- Structured output with schema validation
- Batch inference with deterministic settings

## First questions at reveal
- What is the exact target output format?
- Is external API usage allowed?
- Latency / token / cost constraints?
- Public/private test split?
